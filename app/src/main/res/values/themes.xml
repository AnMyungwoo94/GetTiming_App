<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GetTiming_App" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <!-- 타이틀바 없애기. -->
        <item name="windowNoTitle">true</item>

    </style>
    
    <style name="Theme.GetTiming_App.SplashScreen" parent="Theme.SplashScreen">
        <!-- 스플래시의 백그라운드 색 -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <!-- 위에 적용된 아이콘이 끝나고 어떻게 할거냐 -> 기본값으로 다시 되돌림 -->
        <item name="postSplashScreenTheme">@style/Theme.GetTiming_App</item>
    </style>

</resources>